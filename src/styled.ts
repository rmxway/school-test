import styled from 'styled-components';

export const LastInfo = styled.pre`
	padding: 10px;
	width: auto;
	max-width: 100%;
	overflow-y: auto;
`;

export const List = styled.ul`
    margin: 0;
    margin-bottom: 40px;
    padding-left: 20px;

    li {
        margin-bottom: 10px;
    }
`;
